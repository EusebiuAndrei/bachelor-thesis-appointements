import User from './User';
import Role from './Role';
import Appointment from './Appointment';
import AppointmentStatus from './AppointmentStatus';

const Models = [User, Role, Appointment, AppointmentStatus];

export default Models;
